# Audit SEO complet — ProducerHit

**Date :** 17 juin 2026  
**Domaine :** https://www.producerhit.com  
**Stack :** SPA React (Vite) + prerender bots (Vercel) + sitemaps statiques

---

## Synthèse exécutive

| Zone | État avant | État après audit |
|------|------------|------------------|
| Sitemap | 227 URLs, lastmod obsolète (mai) | 227 + index, lastmod core à jour |
| Robots.txt | OK, 2 sitemaps | + `sitemap-index.xml` |
| Metadata runtime | SeoBootstrap complet | Loop délégué, 404 noindex, FAQ i18n |
| Open Graph | PNG OK | + dimensions 1200×630 + alt |
| JSON-LD | EN hardcodé home/pricing | FAQ FR/EN, Organization, Product pricing |
| Canonical | OK (explore→community) | Inchangé |
| Internal linking | Footer riche | + lien `/trending` produit |
| CWV | Fonts render-blocking | Chargement async Inter |
| Pages indexables | 404 indexées (bug) | 404 + profils `/u/` noindex |

**Score global estimé : 82/100** (technique solide, contenu long-tail déjà massif).

---

## 1. Sitemap

### Fichiers

| Fichier | URLs | Rôle |
|---------|------|------|
| `public/sitemap-index.xml` | 2 sitemaps | **Nouveau** — point d’entrée Search Console |
| `public/sitemap.xml` | 227 | Pages marketing, blog, community, comparatifs |
| `public/sitemap-loops.xml` | 100 | Tracks publics `/loop/:id` |

### Contenu vérifié ✅

- `/`, `/blog`, `/community`, `/trending`, `/pricing`, `/legal`
- 138 landing pages SEO (`SEO_PAGE_PATHS`)
- 47 articles blog
- 8 pages community (hub + 6 vibes + trending)
- Comparatifs : Suno, Udio, Beatoven, Soundraw, Mubert, Loudly, etc.

### Pipeline build

```bash
npm run generate:seo
```

Scripts : `sync-seo-sitemap`, `sync-blog-sitemap`, `sync-community-sitemap`, **`sync-sitemap-lastmod`**, **`sync-sitemap-index`**, RSS, prerender Vercel.

### Recommandations restantes

- [ ] Soumettre `sitemap-index.xml` dans Google Search Console & Bing Webmaster
- [ ] Regénérer `sitemap-loops.xml` en CI avec clés Supabase (actuellement stale si pas de `.env`)
- [ ] IndexNow : échec local SSL — OK en prod Vercel si certificats valides

---

## 2. Robots.txt

**Fichier :** `public/robots.txt`

```
Allow: /
Disallow: /dashboard, /library, /sample-lab, /voice-studio, /settings, /admin, /auth, /theme-preview
Sitemap: sitemap-index.xml + sitemap.xml + sitemap-loops.xml
```

✅ Zones privées couvertes  
✅ App auth/settings exclus  
⚠️ `/auth/callback` et `/u/*` gérés via `noindex` runtime (pas Disallow — permet crawl léger)

---

## 3. Metadata (title, description, robots)

**Moteur :** `src/components/SeoBootstrap.tsx` + `src/i18n/seo.ts` (10 locales UI)

| Route | Title/Description | Robots |
|-------|-------------------|--------|
| `/` | i18n `homeTitle` | index |
| `/pricing`, `/legal` | i18n | index |
| `/community`, `/trending`, vibes | i18n + CollectionPage | index |
| `/blog`, `/blog/:slug` | Contenu article | index |
| 138 landings SEO | `seoPages.ts` EN/FR | index |
| Comparatifs | `seoComparisons.ts` EN/FR | index |
| `/loop/:id` | **`setLoopPageSeo`** (genre, BPM, OG dynamique) | index |
| `/dashboard`, `/auth`, etc. | générique | **noindex** |
| `*` (404) | générique | **noindex** (corrigé) |
| `/u/:username` | générique | **noindex** (pas en sitemap) |

---

## 4. Open Graph & Twitter

- Image par défaut : `/og-image.png` (1200×630)
- Loops : `/api/og-loop?id=…` (cover dynamique)
- **Ajouté :** `og:image:width`, `og:image:height`, `og:image:alt`
- Twitter : `summary_large_image`

---

## 5. Structured Data / JSON-LD

| Page | Schémas |
|------|---------|
| `index.html` (bootstrap) | Organization, WebSite + SearchAction, WebApplication |
| Home | WebApplication + **FAQPage i18n** |
| Pricing | FAQPage i18n + **Product/AggregateOffer USD** |
| Blog index | Blog + BlogPosting list |
| Blog post | BlogPosting + BreadcrumbList |
| Comparatif | FAQPage + WebPage + BreadcrumbList |
| Landing SEO | FAQPage + WebPage |
| Community/Trending | CollectionPage |
| Loop public | MusicRecording + WebPage + BreadcrumbList (`pk-loop-jsonld`) |

**Corrections livrées :**
- Prix JSON-LD alignés USD (`$8 / $24 / $47`) au lieu d’EUR incorrect
- FAQ home/pricing localisées FR/EN (`seoFaqJsonLd.ts`, `getPricingFaqs`)

---

## 6. Canonical URLs

- Canonical runtime via `<link rel="canonical">`
- `/explore` → 301 permanent vers `/community` (`vercel.json`)
- Paires EN/FR SEO : canonical par locale (`getSeoPageCanonicalPath`, `getComparisonCanonicalPath`)
- hreflang : 10 locales UI + paires EN/FR marketing + RSS alternates

---

## 7. Internal linking

| Zone | Liens |
|------|-------|
| `LandingFooter` | Produit, comparatifs, 16+ guides SEO, légal |
| `CommunitySeoFooter` | Vibes, trending, guides, blog |
| Landing FAQ / CTA | `/auth`, `/pricing`, `/dashboard` |
| Blog | Cross-links articles |
| **Ajouté** | `/trending` dans colonne Produit footer |

**Manques mineurs :**
- Profils créateurs `/u/` non maillés (noindex volontaire)
- Navbar marketing pourrait ajouter lien Blog (optionnel)

---

## 8. Core Web Vitals (audit statique)

| Signal | Observation | Action |
|--------|-------------|--------|
| LCP | Fonts Google bloquantes | ✅ Chargement async Inter (`media=print` trick) |
| INP | Dashboard lourd (4500+ lignes) | noindex — hors SEO |
| CLS | Thèmes Prism/Warm Glass | Stable sur landing |
| TTFB | Vercel edge + prerender bots | OK |

**Prerender bots :** Google/Bing reçoivent HTML enrichi pour `/community`, `/trending`, vibes, 168 landings SEO, `/loop/:id` (social).

**Recommandations :**
- [ ] Mesurer CrUX / PageSpeed sur `/`, `/ai-beat-generator`, `/community`
- [ ] Lazy-load GTM (déjà noscript fallback)
- [ ] Preconnect Supabase storage si LCP images community

---

## 9. Pages indexables — inventaire

**Indexables (~327 URLs sitemap + loops dynamiques) :**
- 1 home
- 4 hub (community, trending, blog index, pricing, legal)
- 138 landings EN/FR
- ~20 comparatifs EN/FR
- 47 blog posts
- 6 vibes community
- 100+ loops publics

**Non indexables (volontaire) :**
- Studio : dashboard, library, settings, sample-lab, voice-studio, admin, theme-preview
- Auth : `/auth`, `/auth/callback`
- Profils : `/u/:username` (future phase SEO créateurs)
- 404 : vraie page avec noindex

**Soft 404 :** corrigé — `NotFound.tsx` affiche 404 UI + noindex (HTTP 200 SPA — limite Vercel classique).

---

## 10. Fichiers modifiés (cette session)

- `src/components/SeoBootstrap.tsx`
- `src/lib/seoFaqJsonLd.ts` (nouveau)
- `index.html` — JSON-LD bootstrap, OG dimensions, fonts async
- `public/robots.txt`
- `public/sitemap-index.xml` (nouveau)
- `public/sitemap.xml` — lastmod core
- `src/components/landing/LandingFooter.tsx`
- `scripts/sync-sitemap-index.mjs`, `sync-sitemap-lastmod.mjs`
- `package.json` — pipeline `generate:seo`

---

## Prochaines étapes prioritaires

1. Search Console : propriété + soumission sitemap-index
2. CI : `generate:seo` avec secrets Supabase pour loops RSS/sitemap
3. Contenu : 2 articles/mois ciblant clusters Suno alternatives + type beat
4. Profils créateurs : indexation sélective si contenu public riche
5. HTTP 404 réel : middleware Vercel edge (nice-to-have)
