---
name: ph-viral-render
description: Free CPU video render — viral ACE + YouTube Shorts batch ($0)
---

# VIRAL Render (Hermes)

Read `TOOLKIT.md`. Use terminal from repo:

```
cd "C:\Users\dylar\Documents\ProducerKit AI - Cursor 2"
node --use-system-ca scripts/viral/viral-agent-run.mjs status
node --use-system-ca scripts/viral/viral-agent-run.mjs pipeline
```

Report: commands run, loop IDs, failures, next step for publish agent.

If pipeline partial fail, run steps individually (seed, generate, youtube-batch).
