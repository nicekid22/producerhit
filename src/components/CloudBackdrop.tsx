/** Calques mesh Cloud — photo moodboard + teinte + grain organique. */
export function CloudBackdrop() {
  return (
    <>
      <div className="pk-cloud-photo absolute inset-0 scale-[1.12]" aria-hidden />
      <div className="pk-cloud-tint absolute inset-0" aria-hidden />
      <div className="pk-cloud-mesh absolute inset-0" aria-hidden />
      <div className="pk-cloud-bloom absolute inset-0" aria-hidden />
      <div className="pk-cloud-grain absolute inset-0" aria-hidden />
      <div className="pk-cloud-vignette absolute inset-0" aria-hidden />
    </>
  );
}
