import { CommercialLicenseModal } from "@/components/license/CommercialLicenseModal";

/** Modal licence commerciale par titre — monté une fois dans l'app. */
export function CommercialLicenseHost() {
  return <CommercialLicenseModal />;
}
