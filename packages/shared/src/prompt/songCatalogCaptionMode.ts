/**
 * Song + genre catalogue : envoyer les tags ACE (buildRichAceCaption) au lieu de
 * sample_mode avec la phrase FR « Une chanson… » (interprétée chanson française par ACE).
 *
 * Rollback : passer à `false` et redéployer — comportement sample_mode d'avant.
 */
export const SONG_CATALOG_CAPTION_TAGS_ENABLED = true;
